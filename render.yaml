# Render Blueprint for the Mitingu website (static site)
# Docs: https://render.com/docs/blueprint-spec
services:
  - type: web
    name: mitingu-website
    runtime: static
    buildCommand: npm run build
    staticPublishPath: ./dist/client
